name: PMD Temperature Scraper

on:
  schedule:
    # Runs every 5 minutes FOR TESTING (Diagnostic Mode)
    # Once verified, change this back to '0 11,15 * * *'
    - cron: '*/5 * * * *'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install playwright pandas openpyxl
          python -m playwright install chromium --with-deps

      - name: Run Scraper
        run: python fetch_pmd_temps.py
        env:
          PYTHONUNBUFFERED: "1"

      - name: Save Results
        if: always()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Sync with remote
          git pull origin main --rebase
          
          # Add all new files
          git add .
          
          # Commit and push only if changes exist
          if [[ -n "$(git status --short)" ]]; then
            git commit -m "Auto-update: Data/Screenshot - $(date +'%Y-%m-%d %H:%M')"
            git push origin main
          else
            echo "No changes detected, nothing to push."
          fi
